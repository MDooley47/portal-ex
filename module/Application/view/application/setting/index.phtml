<?php

$title = "All Settings";
$escaper = new Zend\Escaper\Escaper('utf-8');
$this->headTitle($title);

$i = 0;
$data = [];
foreach ($settings as $setting) {
    $data[$i++] = $setting;
}

?>

<h1><?= $escaper->escapeHTML($title) ?></h1>

<hr>
<span class="text-center">Below is a view of what is in the db</span>
<hr>
<table class="table">
    <tr>
    <th>ID</th>
    <th>Slug</th>
    <th>Data</th>
    <th>Actions</th>
</tr>
<?php foreach ($data as $setting) : ?>
    <tr>
        <td><?= $escaper->escapeHtml($setting->id) ?></td>
        <td><?= $escaper->escapeHtml($setting->slug) ?></td>
        <td><?= $escaper->escapeHtml($setting->data) ?></td>
        <td>
            <a href="<?= $this->url('setting', ['action' => 'edit', 'slug' => $setting->slug]) ?>">Edit</a>
            |
            <a class="post" href="<?= $this->url('setting', ['action' => 'delete', 'slug' => $setting->slug]) ?>">Delete</a>
        </td>
    </tr>
<?php endforeach;?>
</table>
<p>
    <a href="<?= $this->url('setting', ['action' => 'add']) ?>">Add new settings</a>
</p>
