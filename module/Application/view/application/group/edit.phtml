<?php
    $escaper = new Zend\Escaper\Escaper('utf-8');
    $this->headTitle($title = 'Edit Group');

    $tables = new Tables($tables_array);

?>

<h1><?= $escaper->escapeHTML($title); ?></h1>

<?php
    $form->setAttribute('action', $this->url('group', ['action' => 'edit', 'slug' => $slug]));
    $form->setAttribute('enctype', 'multipart/form-data');
    $form->prepare();
?>
    <?= $this->form()->openTag($form) ?>
    <?= $this->formHidden($this->form->get('slug')); ?>
    <?= $this->formRow($this->form->get('name')); ?>
    <br>
    <?= $this->formRow($this->form->get('description')); ?>

<?= $this->formRow($this->form->get('submit')); ?>

<?= $this->form()->closeTag(); ?>
